[package]
name = "hnd-password-generator"
description = "A configurable and secure password generation library with strength estimation"
version = "0.1.2"
edition = { workspace = true }
license = { workspace = true }
repository = { workspace = true }

[dependencies]
rand = "0.10.2"
thiserror = { workspace = true }
